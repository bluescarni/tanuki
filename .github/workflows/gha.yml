name: GitHub CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  conda_gcc_10:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: bash tools/gha_conda_gcc_10.sh
