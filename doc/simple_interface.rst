.. _simple_interface:

A simple interface
==================

The minimal example shown in the :ref:`previous tutorial <hello_world>` was not very
interesting, as the interface we type-erased did not define any behaviour other than
destruction.

